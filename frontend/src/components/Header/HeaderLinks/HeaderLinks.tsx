import { Button, HStack, Link } from "@chakra-ui/react";
import { FC } from "react";

const HeaderLinks: FC = props => {
  return (
    <HStack spacing={7} textColor="white">
      {/*content here*/}
    </HStack>
  );
};
export default HeaderLinks;
